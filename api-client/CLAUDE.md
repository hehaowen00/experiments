# Project: Conduit (Electron + Solid.js)

## Rules

- Use Solid.js `createStore` for component state with multiple related fields instead of many individual `createSignal` calls. Group related state into stores (e.g. sidebar state, form state, query state).
- Keep task completion summaries short — 2-3 bullet points max, no repeating what was already discussed.
