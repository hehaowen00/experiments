# Directory

`event-bus-v2` - typed buffered event bus in go

`git-server` - proof of concept git server that can host https and ssh git repos

`go-monkey` - monkey interpreter in go

`search-engine` - proof of concept search engine in go for learning tfidf, bm25 and vector embedding
